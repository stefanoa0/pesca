<h2>Desembarque -> Tipos de Pagamentos</h2>
<form method="post" id="tipo-pagamento">
    <fieldset id="dadosEmbarcacao"></fieldset>

    <a id="ancora_tipo-pagamento"></a>
    <fieldset class="noEditable"><legend>Tipos de Pagamentos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Tipos de Pagamentos:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "tipo-pagamento", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "tipo-pagamento", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Pagamento" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="dataTable" class="display" cellspacing="0" width="100%">
            <thead>
                <tr>
                <th width="15%">ID </th>
                <th width="85%">Pagamento</th>
                <th></th>
                <th></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->assignTipoPagamento as $key => $dados): ?>
                    <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tpg_id'] ?></td>
                    <td><?= $dados['tpg_pagamento'] ?></td>
                    <td >
                        <a onclick="jsUpdateDynamic('<?= $dados['tpg_id'] ?>', '<?= $dados['tpg_pagamento'] ?>', document.getElementById('tipo-pagamento'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td >
                        <a onclick="jsDeleteDynamic(<?= $dados['tpg_id'] ?>, '<?= $this->url(array("controller" => "tipo-pagamento", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>        
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "tipo-pagamento", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>