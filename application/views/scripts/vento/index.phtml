
    <h2>Desembarque->Vento</h2>

    <a href="<?= $this->url(array("controller" => "vento", "action" => "novo"), null, true) ?>" id="botao-ancora">Novo</a>
    <fieldset id="dadosDesembarque"></fieldset>
    <table>
        <tr>
            <th>ID</th>
            <th>Vento</th>
        </tr>

        <?php
        foreach ($this->dados as $key => $vento):
            ?>
            <tr>
                <td><?= $vento['vnt_id'] ?></td>
                <td><?= $vento['vnt_forca'] ?></td>

                <td class="td-botao">
                    <a href="<?= $this->url(array("controller" => "vento", "action" => "editar", "id" => $vento['vnt_id']), null, true) ?>">
                        <ul id="icons" class="ui-widget ui-helper-clearfix">
                            <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                        </ul>
                    </a>
                </td>

                <td class="td-botao">
                    <input type="button" value="Excluir" onclick="beforeDelete('<?= $this->url(array("controller" => "vento", "action" => "excluir", "id" => $vento['vnt_id']), null, true) ?>')">
                </td>

            </tr>
        <?php endforeach; ?>
    </table>
    <br/><br/>
    <a href="<?= $this->url(array("controller" => "vento", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório</a>
