<h2>Pescador Especialista -> Tipo de Residencia</h2>
<form method="post">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_residencia"></a>
    <fieldset class="noEditable"><legend>Tipo de Residencia:</legend>
        <fieldset class="noEditable"><legend>Adicionar Tipo de Residencia:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "residencia", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "residencia", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Tipo de Residencia</th>
            </tr>
            <?php foreach ($this->assignResidencia as $key => $dadosResidencia): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosResidencia['tre_id'] ?></td>
                    <td><?= $dadosResidencia['tre_residencia'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dadosResidencia['tre_id'] ?>', '<?= $dadosResidencia['tre_residencia'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dadosResidencia['tre_id'] ?>, '<?= $this->url(array("controller" => "residencia", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "residencia", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>