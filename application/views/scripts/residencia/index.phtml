<h2>Pescador Especialista -> Tipo de Residencia</h2>
<form method="post" id="residencia">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_residencia"></a>
    <fieldset class="noEditable"><legend>Tipo de Residencia:</legend>
        <fieldset class="noEditable"><legend>Adicionar Tipo de Residencia:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "residencia", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "residencia", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Tipo de Residencia</th>
            </tr>
            <?php foreach ($this->assignResidencia as $key => $dadosResidencia): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosResidencia['tre_id'] ?></td>
                    <td><?= $dadosResidencia['tre_residencia'] ?></td>
                    <td class="td-botao">
                        <a name="Editar" onclick="jsUpdateDynamic('<?= $dadosResidencia['tre_id'] ?>', '<?= $dadosResidencia['tre_residencia'] ?>', document.getElementById('residencia'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a  name="Excluir"  onclick="jsDeleteDynamic(<?= $dadosResidencia['tre_id'] ?>, '<?= $this->url(array("controller" => "residencia", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "residencia", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>