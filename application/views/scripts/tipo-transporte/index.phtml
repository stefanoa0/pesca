<h2>Pescador Especialista -> Transportes</h2>
<form method="post">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_tipo-transporte"></a>
    <fieldset class="noEditable"><legend>Transportes:</legend>
        <fieldset class="noEditable"><legend>Adicionar Transportes:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "tipo-transporte", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "tipo-transporte", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Transportes</th>
            </tr>
            <?php foreach ($this->assignTipoTransporte as $key => $dadosTipoTransporte): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosTipoTransporte['ttr_id'] ?></td>
                    <td><?= $dadosTipoTransporte['ttr_transporte'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dadosTipoTransporte['ttr_id'] ?>', '<?= $dadosTipoTransporte['ttr_transporte'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dadosTipoTransporte['ttr_id'] ?>, '<?= $this->url(array("controller" => "tipo-transporte", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "tipo-transporte", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>