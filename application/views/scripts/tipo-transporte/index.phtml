<h2>Pescador Especialista -> Transportes</h2>
<form method="post" id="tipo-transporte">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_tipo-transporte"></a>
    <fieldset class="noEditable"><legend>Transportes:</legend>
        <fieldset class="noEditable"><legend>Adicionar Transportes:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "tipo-transporte", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "tipo-transporte", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Transportes</th>
            </tr>
            <?php foreach ($this->assignTipoTransporte as $key => $dadosTipoTransporte): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosTipoTransporte['ttr_id'] ?></td>
                    <td><?= $dadosTipoTransporte['ttr_transporte'] ?></td>
                    <td class="td-botao">
                        <a name="Editar" onclick="jsUpdateDynamic('<?= $dadosTipoTransporte['ttr_id'] ?>', '<?= $dadosTipoTransporte['ttr_transporte'] ?>', document.getElementById('tipo-transporte'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a  name="Excluir"  onclick="jsDeleteDynamic(<?= $dadosTipoTransporte['ttr_id'] ?>, '<?= $this->url(array("controller" => "tipo-transporte", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "tipo-transporte", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>