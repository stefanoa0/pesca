<h2>Pescador Especialista -> Recursos</h2>
<form method="post">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_recursos"></a>
    <fieldset class="noEditable"><legend>Recursos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Recursos:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "recursos", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "recursos", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Recursos</th>
            </tr>
            <?php foreach ($this->assignRecurso as $key => $dadosRecurso): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosRecurso['trec_id'] ?></td>
                    <td><?= $dadosRecurso['trec_recurso'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dadosRecurso['trec_id'] ?>', '<?= $dadosRecurso['trec_recurso'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dadosRecurso['trec_id'] ?>, '<?= $this->url(array("controller" => "recursos", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "recursos", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>