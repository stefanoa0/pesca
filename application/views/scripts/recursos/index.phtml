<h2>Pescador Especialista -> Recursos</h2>
<form method="post" id="recursos">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_recursos"></a>
    <fieldset class="noEditable"><legend>Recursos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Recursos:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "recursos", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "recursos", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Recursos</th>
            </tr>
            <?php foreach ($this->assignRecurso as $key => $dadosRecurso): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosRecurso['trec_id'] ?></td>
                    <td><?= $dadosRecurso['trec_recurso'] ?></td>
                    <td class="td-botao">
                        <a name="Editar" onclick="jsUpdateDynamic('<?= $dadosRecurso['trec_id'] ?>', '<?= $dadosRecurso['trec_recurso'] ?>', document.getElementById('recursos'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a  name="Excluir"  onclick="jsDeleteDynamic(<?= $dadosRecurso['trec_id'] ?>, '<?= $this->url(array("controller" => "recursos", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "recursos", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>