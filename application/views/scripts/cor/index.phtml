<h2>Desembarque -> Cores</h2>
<form method="post" id="cor">
    <fieldset id="dadosEmbarcacao"></fieldset>

    <a id="ancora_cor"></a>
    <fieldset class="noEditable"><legend>Cores:</legend>
        <fieldset class="noEditable"><legend>Adicionar Cores:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "cor", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "cor", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Cor" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="dataTable" class="display" cellspacing="0" width="100%">
            <thead>
                <tr>
                <th>ID </th>
                <th>Cor</th>
                <th></th>
                <th></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->assignCor as $key => $dados): ?>
                    <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tcor_id'] ?></td>
                    <td><?= $dados['tcor_cor'] ?></td>
                    <td >
                        <a onclick="jsUpdateDynamic('<?= $dados['tcor_id'] ?>', '<?= $dados['tcor_cor'] ?>', document.getElementById('cor'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td >
                        <a onclick="jsDeleteDynamic(<?= $dados['tcor_id'] ?>, '<?= $this->url(array("controller" => "cor", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "cor", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>