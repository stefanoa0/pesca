<h2>Desembarque -> Materiais</h2>
<form method="post" id="material">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="anmateriala_material"></a>
    <fieldset class="noEditable"><legend>Materiais:</legend>
        <fieldset class="noEditable"><legend>Adicionar Materiais:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adcionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "material", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "material", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Materiais" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Material</th>
            </tr>
            <?php foreach ($this->assignMaterial as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tmt_id'] ?></td>
                    <td><?= $dados['tmt_material'] ?></td>
                    <td class="td-botao">
                        <a onclick="jsUpdateDynamic('<?= $dados['tmt_id'] ?>', '<?= $dados['tmt_material'] ?>', document.getElementById('material'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a onclick="jsDeleteDynamic(<?= $dados['tmt_id'] ?>, '<?= $this->url(array("controller" => "material", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "material", "action" => "relatorio"), null, true) ?>" id="botao-anmateriala" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>