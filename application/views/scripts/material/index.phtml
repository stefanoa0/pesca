<h2>Desembarque -> Materiais</h2>
<form method="post">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="anmateriala_material"></a>
    <fieldset class="noEditable"><legend>Materiais:</legend>
        <fieldset class="noEditable"><legend>Adicionar Materiais:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "material", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "material", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Materiais" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Material</th>
                <th width="5%"></th>
                <th width="5%"></th>
            </tr>
            <?php foreach ($this->assignMaterial as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tmt_id'] ?></td>
                    <td><?= $dados['tmt_material'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dados['tmt_id'] ?>', '<?= $dados['tmt_material'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dados['tmt_id'] ?>, '<?= $this->url(array("controller" => "material", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "material", "action" => "relatorio"), null, true) ?>" id="botao-anmateriala" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>