<h2>Pescador Especialista -> Motivo da Pesca</h2>
<form method="post">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_motivo-pesca"></a>
    <fieldset class="noEditable"><legend>Motivo da Pesca:</legend>
        <fieldset class="noEditable"><legend>Adicionar Motivo da Pesca:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "motivo-pesca", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "motivo-pesca", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Motivo da Pesca</th>
            </tr>
            <?php foreach ($this->assignMotivoPesca as $key => $dadosMotivoPesca): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosMotivoPesca['tmp_id'] ?></td>
                    <td><?= $dadosMotivoPesca['tmp_motivo'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dadosMotivoPesca['tmp_id'] ?>', '<?= $dadosMotivoPesca['tmp_motivo'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dadosMotivoPesca['tmp_id'] ?>, '<?= $this->url(array("controller" => "motivo-pesca", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "motivo-pesca", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>