<h2>Pescador Especialista -> Motivo da Pesca</h2>
<form method="post" id="motivo-pesca">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_motivo-pesca"></a>
    <fieldset class="noEditable"><legend>Motivo da Pesca:</legend>
        <fieldset class="noEditable"><legend>Adicionar Motivo da Pesca:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "motivo-pesca", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "motivo-pesca", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Motivo da Pesca</th>
            </tr>
            <?php foreach ($this->assignMotivoPesca as $key => $dadosMotivoPesca): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosMotivoPesca['tmp_id'] ?></td>
                    <td><?= $dadosMotivoPesca['tmp_motivo'] ?></td>
                    <td class="td-botao">
                        <a name="Editar" onclick="jsUpdateDynamic('<?= $dadosMotivoPesca['tmp_id'] ?>', '<?= $dadosMotivoPesca['tmp_motivo'] ?>', document.getElementById('motivo-pesca'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a  name="Excluir"  onclick="jsDeleteDynamic(<?= $dadosMotivoPesca['tmp_id'] ?>, '<?= $this->url(array("controller" => "motivo-pesca", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "motivo-pesca", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>