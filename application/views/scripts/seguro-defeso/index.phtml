<h2>Pescador Especialista -> Seguro Defeso</h2>
<form method="post">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_seguro-defeso"></a>
    <fieldset class="noEditable"><legend>Seguro Defeso:</legend>
        <fieldset class="noEditable"><legend>Adicionar Seguro Defeso:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "seguro-defeso", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "seguro-defeso", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Seguro Defeso</th>
            </tr>
            <?php foreach ($this->assignSeguroDefeso as $key => $dadosSeguroDefeso): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosSeguroDefeso['tsd_id'] ?></td>
                    <td><?= $dadosSeguroDefeso['tsd_seguro'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dadosSeguroDefeso['tsd_id'] ?>', '<?= $dadosSeguroDefeso['tsd_seguro'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dadosSeguroDefeso['tsd_id'] ?>, '<?= $this->url(array("controller" => "seguro-defeso", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "seguro-defeso", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>