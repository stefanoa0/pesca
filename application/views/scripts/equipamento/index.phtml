<h2>Desembarque -> Equipamentos</h2>
<form method="post" id="equipamento">
    <fieldset id="dadosEmbarcacao"></fieldset>

    <a id="anequipamentoa_equipamento"></a>
    <fieldset class="noEditable"><legend>Equipamentos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Equipamentos:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "equipamento", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "equipamento", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Equipamento" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="dataTable" class="display" cellspacing="0" width="100%">
            <thead>
                <tr>
                <th>ID </th>
                <th>Equipamento</th>
                <th></th>
                <th></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->assignEquipamento as $key => $dados): ?>
                    <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['teq_id'] ?></td>
                    <td><?= $dados['teq_equipamento'] ?></td>
                    <td >
                        <a onclick="jsUpdateDynamic('<?= $dados['teq_id'] ?>', '<?= $dados['teq_equipamento'] ?>', document.getElementById('equipamento'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td >
                        <a onclick="jsDeleteDynamic(<?= $dados['teq_id'] ?>, '<?= $this->url(array("controller" => "equipamento", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "equipamento", "action" => "relatorio"), null, true) ?>" id="botao-anequipamentoa" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>