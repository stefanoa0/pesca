<h2>Desembarque -> Equipamentos</h2>
<form method="post">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="anequipamentoa_equipamento"></a>
    <fieldset class="noEditable"><legend>Equipamentos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Equipamentos:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "equipamento", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "equipamento", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Equipamento" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Equipamento</th>
                <th width="5%"></th>
                <th width="5%"></th>
            </tr>
            <?php foreach ($this->assignEquipamento as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['teq_id'] ?></td>
                    <td><?= $dados['teq_equipamento'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dados['teq_id'] ?>', '<?= $dados['teq_equipamento'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dados['teq_id'] ?>, '<?= $this->url(array("controller" => "equipamento", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "equipamento", "action" => "relatorio"), null, true) ?>" id="botao-anequipamentoa" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>