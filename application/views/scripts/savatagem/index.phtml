<h2>Desembarque -> Savatagem</h2>
<form method="post" id="savatagem">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="ansavatagema_savatagem"></a>
    <fieldset class="noEditable"><legend>Savatagem:</legend>
        <fieldset class="noEditable"><legend>Adicionar Savatagem:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "savatagem", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "savatagem", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Savatagem" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Savatagem</th>
            </tr>
            <?php foreach ($this->assignSavatagem as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tsav_id'] ?></td>
                    <td><?= $dados['tsav_savatagem'] ?></td>
                    <td class="td-botao">
                        <a onclick="jsUpdateDynamic('<?= $dados['tsav_id'] ?>', '<?= $dados['tsav_savatagem'] ?>', document.getElementById('savatagem'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a onclick="jsDeleteDynamic(<?= $dados['tsav_id'] ?>, '<?= $this->url(array("controller" => "savatagem", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "savatagem", "action" => "relatorio"), null, true) ?>" id="botao-ansavatagema" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>