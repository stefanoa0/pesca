<h2>Desembarque -> Savatagem</h2>
<form method="post">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="ansavatagema_savatagem"></a>
    <fieldset class="noEditable"><legend>Savatagem:</legend>
        <fieldset class="noEditable"><legend>Adicionar Savatagem:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "savatagem", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "savatagem", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Savatagem" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Savatagem</th>
                <th width="5%"></th>
                <th width="5%"></th>
            </tr>
            <?php foreach ($this->assignSavatagem as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tsav_id'] ?></td>
                    <td><?= $dados['tsav_savatagem'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dados['tsav_id'] ?>', '<?= $dados['tsav_savatagem'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dados['tsav_id'] ?>, '<?= $this->url(array("controller" => "savatagem", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "savatagem", "action" => "relatorio"), null, true) ?>" id="botao-ansavatagema" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>