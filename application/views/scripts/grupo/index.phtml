

    <h2>Filogenia->Grupo</h2>

    <a href="<?= $this->url(array("controller" => "grupo", "action" => "novo"), null, true) ?>" id="botao-ancora">Novo</a>
    <fieldset id="Filogenia"></fieldset>
    <table>
        <tr>
            <th>ID</th>
            <th>Grupo</th>
        </tr>

        <?php
        foreach ($this->dados as $key => $grupo):
            ?>
            <tr>
                <td><?= $grupo['grp_id'] ?></td>
                <td><?= $grupo['grp_nome'] ?></td>

                <td class="td-botao">
                    <a href="<?= $this->url(array("controller" => "grupo", "action" => "editar", "id" => $grupo['grp_id']), null, true) ?>">
                        <ul id="icons" class="ui-widget ui-helper-clearfix">
                            <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                        </ul>
                    </a>
                </td>


                <td class="td-botao">
                    <input type="button" value="Excluir" onclick="beforeDelete('<?= $this->url(array("controller" => "grupo", "action" => "excluir", "id" => $grupo['grp_id']), null, true) ?>')">
                </td>

            </tr>
        <?php endforeach; ?>
    </table>
    <br/><br/>
    <a href="<?= $this->url(array("controller" => "grupo", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório</a>
