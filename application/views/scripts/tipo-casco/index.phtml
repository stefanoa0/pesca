<h2>Desembarque -> Cascos das Embarcações</h2>
<form method="post" id="tipo-casco">
    <fieldset id="dadosEmbarcacao"></fieldset>

    <a id="antipo-cascoa_tipo-casco"></a>
    <fieldset class="noEditable"><legend>Cascos das Embarcações:</legend>
        <fieldset class="noEditable"><legend>Adicionar Cascos das Embarcações:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "tipo-casco", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "tipo-casco", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Cascos" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="dataTable" class="display" cellspacing="0" width="100%">
            <thead>
                <tr>
                <th width="15%">ID </th>
                <th width="85%">Cascos</th>
                <th></th>
                <th></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->assignTipoCasco as $key => $dados): ?>
                    <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tcas_id'] ?></td>
                    <td><?= $dados['tcas_tipo'] ?></td>
                    <td >
                        <a onclick="jsUpdateDynamic('<?= $dados['tcas_id'] ?>', '<?= $dados['tcas_tipo'] ?>', document.getElementById('tipo-casco'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td >
                        <a onclick="jsDeleteDynamic(<?= $dados['tcas_id'] ?>, '<?= $this->url(array("controller" => "tipo-casco", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "tipo-casco", "action" => "relatorio"), null, true) ?>" id="botao-antipo-cascoa" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>