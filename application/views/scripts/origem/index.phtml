<h2>Pescador Especialista -> Origem do Pescador</h2>
<form method="post">
<fieldset id="Especialista"></fieldset>

    <a id="ancora_origem"></a>
    <fieldset class="noEditable"><legend>Origem do Pescador:</legend>
        <fieldset class="noEditable"><legend>Adicionar Origem do Pescador:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "origem", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "origem", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Companhia de Pesca" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Origem do Pescador</th>
            </tr>
            <?php foreach ($this->assignOrigem as $key => $dadosOrigem): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dadosOrigem['to_id'] ?></td>
                    <td><?= $dadosOrigem['to_origem'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dadosOrigem['to_id'] ?>', '<?= $dadosOrigem['to_origem'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dadosOrigem['to_id'] ?>, '<?= $this->url(array("controller" => "origem", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "origem", "action" => "relatorio"), null, true) ?>" id="botao-ancora" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>