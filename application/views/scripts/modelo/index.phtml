<h2>Desembarque -> Modelos</h2>
<form method="post" id="modelo">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="anmodeloa_modelo"></a>
    <fieldset class="noEditable"><legend>Modelos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Modelos:</legend>
            <div id="base">
                <input type="button" name="Add" class="busca" value="Adicionar" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "modelo", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "modelo", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="busca" value="Limpar campos" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Modelo" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="85%">Modelo</th>
            </tr>
            <?php foreach ($this->assignModelo as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tmod_id'] ?></td>
                    <td><?= $dados['tmod_modelo'] ?></td>
                    <td class="td-botao">
                        <a onclick="jsUpdateDynamic('<?= $dados['tmod_id'] ?>', '<?= $dados['tmod_modelo'] ?>', document.getElementById('modelo'))">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Editar"><span class="ui-icon ui-icon-pencil"></span></li>
                            </ul>
                        </a>
                    </td>
                    <td class="td-botao">
                        <a onclick="jsDeleteDynamic(<?= $dados['tmod_id'] ?>, '<?= $this->url(array("controller" => "modelo", "action" => "delete"), null, true) ?>')">
                            <ul id="icons" class="ui-widget ui-helper-clearfix">
                                <li class="ui-state-default ui-corner-all" title="Excluir"><span class="ui-icon ui-icon-close"></span></li>
                            </ul>
                        </a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <a href="<?= $this->url(array("controller" => "modelo", "action" => "relatorio"), null, true) ?>" id="botao-anmodeloa" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>