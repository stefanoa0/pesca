<h2>Desembarque -> Modelos</h2>
<form method="post">
<fieldset id="dadosEmbarcacao"></fieldset>

    <a id="anmodeloa_modelo"></a>
    <fieldset class="noEditable"><legend>Modelos:</legend>
        <fieldset class="noEditable"><legend>Adicionar Modelos:</legend>
            <div id="base">
                <input type="button" name="Add" class="button-confirm" onclick="jsInsertDynamic(this.form, '<?= $this->url(array("controller" => "modelo", "action" => "insert"), null, true) ?>', '<?= $this->url(array("controller" => "modelo", "action" => "update"), null, true) ?>')">
                <input type="button" name="Reload" class="button-clear" onclick="jsReloadDynamic(this.form)">

                <input type="text" name="input_valor" placeholder="Modelo" size="40"  />
                <input readonly="" type="text" name="input_id" />
            </div>
        </fieldset>
        <br>

        <table id="tblPerfil">
            <tr>
                <th width="15%">ID </th>
                <th width="65%">Modelo</th>
                <th width="5%"></th>
                <th width="5%"></th>
            </tr>
            <?php foreach ($this->assignModelo as $key => $dados): ?>
                <tr id="rowNumPeril<?= $key ?>">
                    <td><?= $dados['tmod_id'] ?></td>
                    <td><?= $dados['tmod_modelo'] ?></td>
                    <td><input type="button" class="button-edit" width="5%" onclick="jsUpdateDynamic('<?= $dados['tmod_id'] ?>', '<?= $dados['tmod_modelo'] ?>', this.form)"></td>
                    <td><input type="button" class="button-del" width="5%" onclick="jsDeleteDynamic(<?= $dados['tmod_id'] ?>, '<?= $this->url(array("controller" => "modelo", "action" => "delete"), null, true) ?>')"></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </fieldset>
    <fieldset class="noBorder">
        <br><input type="button" class="button-up" width="5%" onclick="scrollTo('top')">
        <a href="<?= $this->url(array("controller" => "modelo", "action" => "relatorio"), null, true) ?>" id="botao-anmodeloa" target="_blank">Gerar Relatório - Lista</a>
    </fieldset>
</form>